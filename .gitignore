node_modules/
.next/
out/
build/
dist/

# secrets — never commit certificates, keys, or service account JSON
secrets/
.env
.env.*
!.env.example

# Firebase build/deploy artifacts (contain the built app + copied env files)
.firebase/
.runtimeconfig.json

npm-debug.log*
*.tsbuildinfo
next-env.d.ts
.DS_Store
test.pkpass
